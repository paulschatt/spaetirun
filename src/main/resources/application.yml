spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spaetirun
    username: spaetirun
    password: spaetirun
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: spaetirun

server:
  port: 8080